# Tasks: Frontend Implementation (Next.js + Tailwind)
 - [x] **Setup:** Initialize Next.js project with TypeScript and Tailwind CSS.
 - [x] **Type Generation:** Generate TypeScript interfaces directly from the Python backend's `openapi.json` to guarantee type safety across the network boundary.
 - [x] **Component (`DashboardLayout`):** Build a responsive grid with two main columns: "Active Operations" and "Recent Processing".
 - [x] **Component (`TransactionCard`):** Design a dense data card showing Merchant, Amount, and a status badge (color-coded for Pending, Suspended, Resolved, Escalated).
 - [x] **Route (`/transactions/[id]`):** Implement the Audit Deep-Dive page.
 - [x] **Component (`AuditTimeline`):** Build a vertical timeline component. Map the array of `arcra_audit_events` to visual nodes.
 - [x] **Component (`SlackInteractionViewer`):** Build a stylized component resembling a chat interface to clearly display the `slack_message_sent` and `slack_reply_received` strings.
 - [x] **Component (`SynthesisMetrics`):** Visualize the agent's `confidence_score` and `synthesis_reasoning` text block.
 - [x] **Feature (Queue + Play/Pause):** Added `GET /api/v1/queue` backend endpoint; `QueuePanel` component; extended `DashboardLayout` with queue props; `isPlaying` state, processing loop, and Play/Pause button in `DashboardPage`; `fetchQueue` and `submitTransaction` API helpers; `QueuedTransactionSchema` / `QueueResponseSchema` Zod schemas; extended `TransactionStatusSchema` with `suspended`, `evidence_found`, `resolved`.
 - [x] **Tests (Queue + Play/Pause):** Added vitest test suites covering all new functionality — `schemas.test.ts` extended (queue schemas, new statuses, new `getStatusMeta` labels); `QueuePanel.test.tsx` (12 tests); `DashboardLayout.test.tsx` (13 tests); `api.test.ts` (17 tests for `fetchQueue`, `submitTransaction`, `fetchActiveTransactions`, `fetchProcessedTransactions`); `DashboardPage.test.tsx` (13 tests for loading/error states, Play/Pause toggle, processing loop behaviour). All 110 tests pass.
 - [x] **Fix (Active Operations empty):** Expanded `_ACTIVE_STATUSES` in `backend/src/api/routes/transactions.py` to include all 7 intermediate statuses (`pending`, `processing`, `suspended`, `policy_check`, `evidence_gathering`, `awaiting_slack`, `evidence_found`). Previously only 3 statuses were included, causing in-flight transactions to be invisible to the active query and SSE stream.
 - [x] **Fix (Audit page 404):** Made `BFF_BASE` in `frontend/src/lib/api.ts` server-side aware — uses `INTERNAL_API_URL` when `typeof window === "undefined"` (React Server Components), falling back to relative URL `""` for client-side fetches. Previously, RSC `fetchTransactionAudit` used a relative URL with no host, causing a network error caught by `notFound()`.
 - [x] **Tests (Audit + intermediate statuses):** Extended `api.test.ts` with 14 new tests — 7 for `fetchTransactionAudit` (success, correct endpoint, special-char encoding, 404, network error, schema mismatch, empty audit trail) and 7 parametrized tests verifying all intermediate active statuses are accepted by `fetchActiveTransactions`. Total tests: 124 passing.
 - [x] **Feature (Reset):** Added `POST /api/v1/reset` backend endpoint (`backend/src/api/routes/reset.py`) that truncates `arcra_ui_read_model` and `arcra_audit_events`, registered in `main.py`. Added `resetState()` API helper in `frontend/src/lib/api.ts` with full Zod validation and structured pino logging. Added Reset button to `DashboardPage` — visible only when `queueEmpty && active.length === 0 && !isPlaying`; clicking it calls `resetState()` then `refreshAll()` to repopulate all three panels from scratch.
 - [x] **Tests (Reset):** Added 7 `resetState` tests to `api.test.ts` (success, correct endpoint/method, 4xx, network error, JSON parse failure, schema mismatch, zero row counts) and 5 Reset button tests to `DashboardPage.test.tsx` (hidden when queue has items, visible when queue empty + no active, hidden when active transactions exist, calls `resetState` on click, re-fetches all panels after click). Fixed pre-existing selector ambiguity in Play-disabled test. Total tests: 136 passing.

- [x] Replace Play button with Reset button (not alongside) when queue is empty and all transactions processed (`canReset`); updated affected DashboardPage test; all 136 tests pass